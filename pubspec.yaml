name: heart_attack_mvp_app
description: "Heart Attack MVP - 심박수 기반 대화 게임"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ── 상태 관리 (State Management) ──
  flutter_riverpod: ^2.6.1       # 중앙 데이터 창고 (Riverpod)

  # ── 카메라 (심박수 측정 엔진) ──
  camera: ^0.11.1                # 카메라 프레임 스트림 및 플래시 제어
  flutter_ppg: ^0.2.4            # 전문 PPG 심박수 측정 패키지 (피크 검출, 이상치 제거, 품질 평가)

  # ── Firebase (실시간 채팅 & 심박수 동기화) ──
  firebase_core: ^3.13.0
  firebase_database: ^11.3.5     # 실시간 데이터 공유 (RTDB)

  # ── 음성 통화 ──
  agora_rtc_engine: ^6.5.2       # Agora 음성 채널 연결

  # ── 애니메이션 ──
  lottie: ^3.3.1                 # 로비 하트 애니메이션
  google_fonts: ^6.2.1           # 프리미엄 폰트 (Outfit, Inter 등)
  share_plus: ^13.1.0
  path_provider: ^2.1.5
  flutter_dotenv: ^6.0.1
  google_generative_ai: ^0.4.7
  speech_to_text: ^7.4.0
  flutter_tts: ^4.2.5
  just_audio: ^0.10.5
  dart_openai: ^6.1.1
  cloud_firestore: ^5.6.12
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see

  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
