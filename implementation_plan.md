# 심박수 측정 알고리즘 전면 개편 기획안 (투 트랙 전략)

본 기획안은 기존의 복잡했던 교착(Deadlock) 방지 로직과 데이터 연관성 검증 로직을 모두 폐기하고, 사용자 행동(UX) 기반의 가장 직관적이고 강력한 **'투 트랙(Two-Track) 전략'**으로 알고리즘을 전면 재설계하는 내용을 담고 있습니다.

---

## 🚀 핵심 설계 철학 (Two-Track Strategy)

심박수 측정 시 발생하는 모든 예외 상황을 오직 **두 가지 기준**으로만 완벽하게 분리하여 처리합니다.

### 트랙 1. 완벽한 초기화 (Reset) : "오직 손을 뗐을 때만"
알고리즘 내부의 데이터 꼬임이나 에러 누적으로 인한 강제 초기화 로직(`_noiseHoldFrameCount` 등)을 **전면 폐기**합니다. 초기화의 결정권은 전적으로 사용자의 물리적 행동에 맡깁니다.

- **발동 조건**: 사용자가 카메라 렌즈에서 손가락을 완전히 떼어 신호 불량(`isGoodSignal == false`) 상태가 감지되었을 때만 발동합니다.
- **초기화 대기 시간**: 손을 뗀 상태가 실제 시간으로 **4초(120프레임)** 동안 지속되면 시스템을 `0.0`으로 완전히 포맷합니다.
- **복귀 기준 (Complete Fresh Start)**: 
  - 초기화가 된 후 다시 손가락을 얹어 측정을 재개할 때, **이전 심박수 데이터와의 연관성은 단 1%도 고려하지 않습니다.** (기존 ±15, ±30 복귀 룰 전면 폐기)
  - 대신 앱을 처음 켰을 때와 완벽하게 동일하게, 10프레임 안정화 후 잡힌 첫 후보값이 **40 이상 100 이하**의 정상 범주에 들어올 때만 측정의 시작점으로 삼는 '초기 로직'으로 완전히 회귀합니다.

### 트랙 2. 돌발 스파이크 무한 방어 (Spike Hold) : "손을 대고 있는 한 버틴다"
손가락을 대고 있는 상태(`isGoodSignal == true`)에서 발생하는 200 BPM 등의 엉뚱한 노이즈나 데이터 부족 현상은, 그 상태가 아무리 오래 지속되더라도 절대 시스템을 초기화시키지 않고 스파이크 룰로 방어합니다.

- **스파이크 판정 기준점**: '직전 원시값'과 비교하던 기존 방식을 폐기하고, **'이전 필터 데이터(`_lastFilteredBpm`)'와 '현재 원시 데이터(`currentBpm`)'를 비교**하는 방식으로 변경하여 기준의 안정성을 극대화합니다.
- **방어 메커니즘**: 
  - 현재 원시값이 직전 필터값과 **18.0 초과**로 차이가 나면 무조건 노이즈(스파이크)로 간주합니다.
  - 이 경우 새로운 값을 철저히 무시하고, **화면에는 기존 필터 데이터를 무한정 유지(Hold)**하며 버팁니다.
  - 진짜 심박수가 변하는 경우(예: 70 ➡️ 110) 0.03초 만에 20 BPM이 변하지 않고 점진적으로 변하기 때문에 스파이크에 걸리지 않고 부드럽게 추적되며, 진짜 에러(예: 70 ➡️ 200)는 영구적으로 차단됩니다.
  - 만약 심각한 에러로 수치가 오랫동안 멈춰있다면, 사용자가 스스로 손가락을 떼어 **트랙 1(완전 초기화)**을 자연스럽게 발동시키도록 유도합니다.
